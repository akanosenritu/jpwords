import React from "react";

type PracticeWithSelectionProps = {
  options: string[],
  answer: string
}

export const PracticeWithSelection: React.FC<PracticeWithSelectionProps> = props => {
  return <div style={{textAlign: "center", width:"100%"}}>

  </div>
}